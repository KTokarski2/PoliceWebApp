<!DOCTYPE html>
<html lang="pl-PL">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link rel="stylesheet" href="/css/style.css">
    <link rel="icon" href="/images/swppp-logo.png" type="image/icon type">
    <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:opsz,wght,FILL,GRAD@20..48,100..700,0..1,-50..200" />
    <title>SWPPP</title>
</head>
<body>

<header>
    <h1><%= __('title')%></h1>
    <a href="/snake"><img src="/images/swppp-logo.png" alt="SWPPP - system logo" width="130" height="100"></a>
</header>

<nav>
    <ul>
        <li><a href="/" class="<%= navLocation == 'main' ? 'active' : '' %>"><%= __('nav.main-page')%></a></li>
        <li><a href="/PoliceOfficer" class="<%= navLocation == 'policeOfficer' ? 'active' : '' %>"><%= __('nav.policeOfficers')%></a></li>
        <li><a href="/Case" class="<%= navLocation == 'case' ? 'active' : ''%>"><%= __('nav.cases')%></a></li>
        <li><a href="/Participation" class="<%= navLocation == 'participation' ? 'active' : '' %>"><%= __('nav.participation')%></a></li>
        <li><a href="/changeLang/pl" class="<%= lang == 'pl' ? 'active' : ''%>">PL</a></li>
        <li><a href="/changeLang/en" class="<%= lang == 'en' ? 'active' : ''%>">EN</a></li>

    </ul>
</nav>
<% if (!loggedUser) { %>

    <div id="login" class="login-div">
        <h5><%= __('log.logging')%></h5>
        <form method="post" action="/login" novalidate>
            <label for="lastName"><%= __('log.lastName') %></label>
            <input type="text" id="loginLastName" name="lastName">

            <label for="loginPassword"><%= __('log.password') %></label>
            <input type="password" id="loginPassword" name="password">

            <input type="submit" class="button-login" value="<%= __('log.login') %>">
            <span id="errors-text"><%= loginError ? loginError : ''%></span>
        </form>
    </div>
<% } else { %>

    <span class="loginInfo"><%= __('log.loginInfo')%> <%= loggedUser.firstName + " " + loggedUser.lastName%></span>
    <a class="button-logout" href="/logout"><%= __('log.logout')%></a>

<% } %>